version: '3'
services:
  php:
    container_name: php
    build: ./docker/php
    entrypoint: /var/www/docker/php/entrypoint.sh
    volumes:
      - .:/var/www
  mailcatcher:
    container_name: mailcatcher
    image: schickling/mailcatcher
    ports:
      - "1080:1080"